instance default Element{

    fn x(){
        return Element{}
    }

    fn y(){
        var a = Element{}
        var run = function(e){ return e }
        var b = run(Element{})
        var c = function(){
            return Element{}
        }

        return { a: a, b: b, c: c }
    }
}
