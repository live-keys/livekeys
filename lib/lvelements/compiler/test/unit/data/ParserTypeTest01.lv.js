export class X extends Element{

    constructor(){
        super()
        X.prototype.__initialize.call(this)
    }
    __initialize(){
    }

    run(){
        var a = 100
        let x = 100
        let y = 20
        let {m, n} = {m: 1, n: 2}
        function test(c,d){}
   }
}