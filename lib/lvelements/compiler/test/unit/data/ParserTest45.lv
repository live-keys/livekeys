component X{
    var x: {
        return this.y.value + this.z.value
    }
    var y: this.z.a + this.z.b
    z: this.m.getValue()
    w: this.getValue()

    t: T{
        var x: {
            var i = this.y
            return i.length
        }
        var y: this.z.a + this.z.b
        z: this.m.getValue()
    }
}
